# Example environment configuration for development

# GitHub repository (format: owner/repo)
GITHUB_REPO=yourusername/your-tool

# Binary name (without extension)
BINARY_NAME=your-tool

# Enable debug logging
DEBUG=1

# Allowed paths (comma-separated)
ALLOWED_PATHS=/home/user/projects,/home/user/documents

# Blocked paths (comma-separated)
BLOCKED_PATHS=/etc,/sys,/proc